{
	"name": "@kruddage/kruddmake",
	"version": "0.0.0",
	"private": true,
	"description": "The KRUDD build language: the Scheme that renders build.ninja from the directory specs, and the shell entry point that drives it. A dependency edge where there used to be a path to krudd.sh — its surface is the operations, not the five .scm modules behind them.",
	"license": "GPL-2.0-or-later",
	"files": [
		"*.scm",
		"kruddmake.sh",
		"run-scheme-tests.sh",
		"run-tests.sh"
	],
	"bin": {
		"kruddmake": "./kruddmake.sh"
	},
	"exports": {
		"./package.json": "./package.json"
	},
	"scripts": {
		"test": "sh ./run-scheme-tests.sh"
	},
	"dependencies": {}
}
