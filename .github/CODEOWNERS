# Code owners — GitHub requests a review from the owner when a matching path
# changes. Order matters: the LAST matching pattern wins for a given file.
# See https://docs.github.com/articles/about-code-owners.

# Default owner for everything not matched more specifically below.
*                       @joshcoyne

# /krudd/ below covers the whole build tree — tool, specs, and the
# modules/plugins sources it owns — but the plugin vtable headers ARE the
# engine ABI (see CLAUDE.md), so guard them with their own explicit line: a
# change to an exported signature is a breaking change.
/krudd/cmake/plugins/include/ @joshcoyne

# Build and CI/release machinery. The root CMakeLists.txt is generated from
# krudd/cmake/CMakeLists.scm now (the .txt itself is gitignored).
/krudd/                 @joshcoyne
/.github/               @joshcoyne

# Version and release-notes sources — must stay consistent.
/VERSION                @joshcoyne
/CHANGELOG.md           @joshcoyne
