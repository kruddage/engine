{
	"name": "@kruddage/abi",
	"version": "0.0.0",
	"private": true,
	"description": "The KRUDD plugin vtables: the C headers every tier includes and nothing links. A declaration and a graph node — the C surface is build.scm's (interface \".\"), and this package exports nothing to JS.",
	"license": "GPL-2.0-or-later",
	"files": [
		"*.h",
		"build.scm"
	],
	"exports": {},
	"dependencies": {}
}
