{
	"//": "Render-diff scene manifest. See README.md for the mode ladder.",
	"//canvas": "CSS selector screenshots are clipped to. The page chrome differs",
	"//canvas2": "by backend on purpose (the header badge reads WEBGL vs WEBGPU),",
	"//canvas3": "so a full-page diff would always fail. Clip to the canvas.",
	"//timing": "capture_frames = rendered frames past first render to settle for",
	"//timing2": "under the deterministic clock (#603); settle_ms = wall-clock",
	"//timing3": "ceiling on reaching that frame, not a fixed wait.",
	"//game": "Every query names a project: a bare URL opens the picker over",
	"//game2": "the default scene, so a scene that wants a board on screen has",
	"//game3": "to ask for one. chess is fetched rather than embedded since",
	"//game4": "#1034 -- default holds the staged slot now -- so the capture",
	"//game5": "waits on one round trip, which settle_ms already covers.",
	"canvas": "#canvas",

	"scenes": [
		{
			"id": "triangle",
			"query": "?renderer=webgpu&game=chess",
			"webgl_query": "?renderer=webgl&game=chess",
			"mode": "self",
			"tolerance": 0.002,
			"capture_frames": 60,
			"settle_ms": 12000,
			"note": "The #570 WGSL triangle probe. Static, so tolerance is tight and the frame count is only there to clear boot. Mode is 'self' because the WebGL path renders the whole engine here, not a triangle -- there is no comparable reference until PR4 registers scene_renderer on the WebGPU path. WebGPU is the page default now, so webgl_query pins the baseline shot to ?renderer=webgl explicitly rather than relying on an unparented default."
		},
		{
			"id": "parity",
			"query": "?renderer=webgpu&game=chess",
			"webgl_query": "?renderer=webgl&game=chess",
			"mode": "diff",
			"tolerance": 0.002,
			"capture_frames": 180,
			"settle_ms": 15000,
			"note": "The parity meter: the whole engine under both backends, WebGL as the reference. This is the scene the 'diff' mode was built for, and it was blocked until scene_renderer ran on the WebGPU path (#595). Capture is now deterministic (#603): the injected clock advances one step per rendered frame and both backends are shot at frame 180 past first render, so the number is reproducible across runs and across backends rather than sampling a random animation phase. EXPECTED TO FAIL until the port finishes -- the number is the point, not the verdict. It should shrink as gaps close: today's diff is dominated by the missing mip chain (#605) and the depth-range convention (#606). render-diff is not run in CI, so a failing scene here costs no one a build; read it as a progress meter you run by hand."
		},
		{
			"id": "bloom-off",
			"query": "?renderer=webgpu&game=chess",
			"webgl_query": "?renderer=webgl&game=chess",
			"mode": "self",
			"tolerance": 0.0,
			"capture_frames": 180,
			"settle_ms": 15000,
			"note": "The other half of #1022, and the half that is easy to break: a scene with NO emissive material must render what it rendered before the bloom pass existed. Chess authors none, so the tick declares no bright pass, no blurs and no half-res transients -- and, just as importantly, everything else is untouched: the scene still renders into the multisampled offscreen target, still resolves, and reaches the backbuffer through the present blit instead of the bloom composite. Anti-aliasing does not depend on whether a scene happens to glow. Tolerance is 0.0 on purpose: this scene is not measuring a rendering, it is asserting that one did not change, and any drift at all is the regression it exists to catch -- an aliased edge would show up here as thousands of differing pixels. The structural half of the same claim is asserted natively in scene_renderer_test.c (test_bloom_follows_emissive), which compares the two frames' declared targets sample count and all -- this is the pixel witness. NEEDS ITS REFERENCE ACCEPTED ONCE: run with --accept on a machine with a browser and a built site, after checking out/bloom-off.webgpu.png against a pre-bloom build."
		}
	]
}
