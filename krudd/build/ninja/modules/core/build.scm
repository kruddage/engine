; SPDX-License-Identifier: GPL-2.0-or-later
;; scm-lint:off
;
; Engine heartbeat — init/tick/shutdown, subsystem manager, and the "index"
; executable that becomes index.html/.js/.wasm under Emscripten.
;
; The WASM-specific shape of the "index" target — the extra plugin_abi.c source,
; the .html suffix, the shell/pre-js and the main-module link flags — is owned
; by the emitter (krudd/build/ninja/ninja.scm: the main_module rule and
; mainflags), not spelled here; version.h / shell.html are generated by
; krudd/build/introspect.scm's configure_file at synthesis time.
;
; (link ...) stays the core libraries only, so the native executable — which
; is never actually built (the engine loop runs only under Emscripten) — links
; cleanly. The plugin modules folded into the single WASM module are named by
; (wasm-modules ...); the emitter compiles each with emcc/em++ and links it into
; index.html. Keep that list in step with register_plugins() in engine.c.
;; scm-lint:on
((library "subsystem"
	(sources "subsystem.c")
	(public "include"))

 (library "subsystem_manager"
	(sources "subsystem_manager.c")
	(public "include"))

 ;; scm-lint:off
 ;; The embedded s7 Scheme runtime. s7.c itself is folded into this archive by
 ;; the emitter (with its own relaxed compile flags — see ninja.scm), so a bare
 ;; (link "script") drags the whole interpreter in for native tests and the
 ;; WASM main module alike.
 ;; scm-lint:on
 (library "script"
	(sources "script.c")
	(public "include")
	(private (raw "../../third_party"))
	(link "log" "m"))

 (executable "index"
	(sources "engine.c")
	(private "include" (raw "${generated}")
		(root "modules/include"))
	(link "subsystem" "subsystem_manager" "log" "memory" "script")
	(wasm-modules "asset_plugin" "scene_plugin" "edit_plugin" "entity_plugin"
		"renderer_webgl" "frame_graph" "scene_renderer" "imgui_plugin"
		"kruddboard"))

 (native-only
	(executable "subsystem_test"
		(sources "subsystem_test.c")
		(link "subsystem"))
	(test "subsystem" "subsystem_test")

	(executable "subsystem_manager_test"
		(sources "subsystem_manager_test.c")
		(link "subsystem_manager"))
	(test "subsystem_manager" "subsystem_manager_test")

	(executable "script_test"
		(sources "script_test.c")
		(link "script"))
	(test "script" "script_test")))
