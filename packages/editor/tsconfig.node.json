{
	"//": "The build-time half: the Vite plugin, the scripts, and the three configs. Node's types live here and only here, so a stray `node:fs` in src/ is a type error rather than something Vite discovers at bundle time. Same strictness as the app — this code decides where the WASM artifact comes from, which is not a place to be relaxed.",
	"compilerOptions": {
		"target": "ES2022",
		"lib": ["ES2022"],
		"module": "ESNext",
		"moduleResolution": "bundler",
		"types": ["node"],

		"strict": true,
		"noUncheckedIndexedAccess": true,
		"noImplicitOverride": true,
		"noFallthroughCasesInSwitch": true,
		"noUnusedLocals": true,
		"noUnusedParameters": true,
		"exactOptionalPropertyTypes": true,
		"verbatimModuleSyntax": true,

		"allowJs": true,
		"checkJs": false,
		"skipLibCheck": true,
		"esModuleInterop": true,
		"resolveJsonModule": true,
		"isolatedModules": true,
		"noEmit": true
	},
	"include": [
		"build",
		"scripts",
		"vite.config.ts",
		"vitest.config.ts",
		"playwright.config.ts"
	]
}
