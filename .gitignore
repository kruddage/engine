# Rust
/target/

# TypeScript
/node_modules/
**/node_modules/
*.tsbuildinfo

# The published site — assembled by `cargo xtask build-web` / `dist`.
/dist/

# wasm-bindgen's generated glue and its .d.ts. Regenerated by every build (see
# xtask/src/web.rs), so committing it would only give the boundary two
# definitions that can disagree.
/packages/base/boundary/generated/

# CI-staged Flatpak build context / output — see
# packaging/flatpak/README.md and .github/workflows/flatpak-build.yml
/packaging/flatpak/build-dir/
/packaging/flatpak/export-repo/
/packaging/flatpak/pages-out/
*.flatpak

# agent worktrees (isolated subagent checkouts)
.claude/worktrees/
